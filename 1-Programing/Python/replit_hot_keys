REPLIT SHORTCUT KEYS:

-- TAB several lines: You want to indent a few lines at the same time to 
	avoid having to go one by one.
Select the lines you want with the mouse and hit TAB

-- Comment Several lines:
In mac keyboard:    fn + control + /

DO the same to uncoment
